# 🛑 HANDS OFF — server-side Claude is actively upgrading kids.html (2026-04-27 ~22:35Z)

> Written by the **server-side Claude** session (the one that owns api.thatcomputerguy26.org).
> Gary asked me to take over the urgent fixes since the news filter was leaking adult content to Ayden in real time and the weather was broken.

## What I'm doing right now

1. **Strict kid-safe news** — already done.
   - Backend: `/api/proxy/news/kids-safe` with massive blocklist (~150 terms). Cache TTL 60s.
   - Frontend: kids.html now hits that endpoint + has its own client-side UNSAFE regex as defense-in-depth.
   - Cache: `_headers` set to `no-cache, no-store, must-revalidate` for kids.html + index.html. `_KIDS_BUILD` stamp + 30-min auto-reload for stale tabs.
   - Verified: 15+/120 items blocked per pass. Only HN/tech/business/science/space items survive.

2. **Weather expansion** — adding Austin IN, Scottsburg IN, Crothersville IN as primary.
   - Austin: 38.7561°N, -85.8089°W
   - Scottsburg: 38.6856°N, -85.7705°W
   - Crothersville: 38.8011°N, -85.8392°W
   - Plus Seymour, Columbus, Louisville fallbacks.

3. **INDOT around Scott County** — filtering proxy by county=scott and adding nearby counties (Jackson, Clark, Washington, Jefferson). Will use existing `/api/proxy/indot/events?county=` endpoint and call it 5x in parallel from frontend.

4. **Gaming feed** — adding a kid-friendly gaming sources card pulled from the existing `/api/feeds/social/reddit/gaming` endpoint with the strict UNSAFE filter applied.

## What I am NOT touching

- Firebase config (your domain — Gary owns project data-44017)
- index.html main page (your build)
- Any ayden's-page-specific components other than kids.html
- The repo structure / netlify.toml / Functions

## What I will commit + deploy

All changes go to:
- `kids.html` (new weather list, INDOT-by-county, gaming card, version bump)
- `_headers` (already updated)

I will:
- `git commit + push` to your GitHub repo
- `netlify deploy --prod` from this folder

After that, the live site at https://aydens-page.netlify.app/kids.html should be safe + functional for Ayden.

## How not to step on my work

If you're about to deploy from your session, **pull first** to avoid overwriting these fixes:

```
cd "C:\Users\American Rental\Desktop\aydens site"
git pull
```

If there's a conflict in kids.html, take MY version — it's the safety-critical one.

## Coordination back-channel

If you need anything server-side (new endpoint, CORS origin, model access, RTDB rules), drop a NOTE_TO_SERVER_CLAUDE.md in this folder and I'll see it next pass.
